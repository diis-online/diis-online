<? // Parameters for Postgres connection...
$postgres_host = "";
$postgres_port = "";
$postgres_database = "";
$postgres_user = "";
$postgres_password = "";

// Change this to "enabled" and bo to https://diis.online/?view=install
// This will install the first time and allow you to create an administrator
// Once you have finished that that then change this back to "disabled"
$allow_install = "disabled"; ?>
